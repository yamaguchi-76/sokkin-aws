<div class="container mt-5">
  <% provide(:title, "案件編集") %>

  <%= render 'form', proposition: @proposition, url: admins_proposition_path(@proposition.id), method: :patch %>

  <div class="text-center">
    <%= link_to '詳細', admins_proposition_path(@proposition.id), class: "mb-3 btn btn-primary" %>
    <%= link_to '戻る', admins_propositions_path, class: "mb-3 btn btn-primary" %>
  </div>
</div>