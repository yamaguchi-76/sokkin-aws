<div class="signUpIndex">
  <div class="signUpIndex__container">
    <h1 class="signUpIndex__title">パスワード再設定</h1>
    <div class="signUpIndex__wrapper">
      <div class="signBox signUp">
        <div class="signBox__wrapper">
          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
            <%= render "companies/shared/error_messages", resource: resource %>
            <p class="signBox__text small">
              ご登録のメールアドレス宛に、パスワード再設定メールを<br>
              送信いたします。メールアドレスを入力してください。
            </p>
            <div class="loginFormItem">
              <%= f.label :email, 'メールアドレス', { class: 'loginFormItem__title' } %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'loginFormItem__item' %>
            </div>
            <%= f.submit '送信', class: 'generalButton bgBrown textWhite', disabled: false, id: :confirm %>
          <% end %>
          <div class="signBox__linkText">
              <%= link_to 'ログイン画面に戻る', companies_home_path, class: 'text' %></li>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
